name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - pymbar
  - openMM
  - torchani

  - openmm-torch
  - openff-toolkit
  - pytorch==1.9.0
  - openmmtools
  - tqdm
  
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
     - git+https://github.com/openmm/openmm-ml.git
  #  - codecov

