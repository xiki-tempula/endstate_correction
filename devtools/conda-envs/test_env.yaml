name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - pymbar
  - openMM
  - torchani
  - pytorch
  - openmm-torch
  - openff-toolkit
  - openmmtools

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

