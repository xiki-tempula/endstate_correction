name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - openmm
  - torchani
  - openmm-torch
  - openff-toolkit
  - openmmtools
  - tqdm
  - pymbar
  - seaborn

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - black

    # Pip-only installs
  - pip:
     - git+https://github.com/openmm/openmm-ml.git
  #  - codecov

