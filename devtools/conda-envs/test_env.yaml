name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - openmm
  - nnpops
  - openmm-torch
  - openff-toolkit
  - openmmtools
  - cudatoolkit=11.3
  - tqdm
  - pymbar
  - seaborn

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - black

    # Pip-only installs
  - pip:
     - git+https://github.com/wiederm/openmm-ml.git
  #  - codecov

