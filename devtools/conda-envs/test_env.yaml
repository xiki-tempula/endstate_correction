name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - openMM
  - torchani
  - openmm-torch
  - openff-toolkit
  - openmmtools
  - tqdm
  - pymbar

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
     - git+https://github.com/openmm/openmm-ml.git
  #  - codecov

