name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - openmm>=8.0
  - openmm-torch
  - openff-toolkit
  - openmmtools
  - nnpops>=0.4
  - cudatoolkit=11.4.2
  - tqdm
  - pymbar>=4.0
  - torchani
  - seaborn
  - pytorch
  - mdtraj
  # Testing
  - pytest
  - pytest-cov
  - codecov
  - black

    # Pip-only installs
  - pip:
     - git+https://github.com/openmm/openmm-ml.git
  #  - codecov
